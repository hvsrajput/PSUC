// title

#include <stdio.h>
#include <math.h>

// declare a function

int main()
{
    int a=3, b=5, c=9, p=10, q=8, r=11, m=5, n=4, x=12, y=2, z=6;

    printf("The number is: %d \n", x/y + z - m*y < y + p/m*a + r);
    printf("The number is: %d \n", n * q <= 3 * m + c / a);
    printf("The number is: %d \n",  (m /n != q - 5) && (b>a));
       

    return 0;
}

// function
